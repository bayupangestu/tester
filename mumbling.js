var string = "ZpglnRxqenU";

function accum(s) {
    var value = 0;
    var upper = s.toUpperCase();
    var char = upper.split('');
    var result = reduce()
    return result;
}

console.log(accum(string));